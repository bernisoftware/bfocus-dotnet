<Project Sdk="Microsoft.NET.Sdk">

  <!-- Exemplo rodável (veja o topo de Program.cs): defina BFOCUS_API_KEY e rode "dotnet run" nesta pasta.
       Fora deste repositório, troque o ProjectReference por:  <PackageReference Include="Bfocus" Version="[0.1.0]" /> -->
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net8.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <IsPackable>false</IsPackable>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="../../src/Bfocus/Bfocus.csproj" />
  </ItemGroup>

</Project>
